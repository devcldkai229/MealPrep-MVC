@{
    ViewData["Title"] = "Delivery Processing Dashboard";
    Layout = "_AdminLayout";
}

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col-12">
            <h2 class="fw-bold mb-3">🚚 Delivery Processing Dashboard</h2>
            <p class="text-muted">Quản lý quy trình giao hàng hàng ngày</p>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="row g-4 mb-4">
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">📋 Daily Orders</h5>
                    <p class="card-text">Xem và quản lý đơn hàng theo ngày</p>
                    <a asp-action="DailyOrders" class="btn btn-primary">View Orders</a>
                </div>
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">🍳 Kitchen Export</h5>
                    <p class="card-text">Xuất danh sách món cần nấu</p>
                    <a asp-action="KitchenExport" class="btn btn-success">Export List</a>
                </div>
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">🚀 Generate Orders</h5>
                    <p class="card-text">Tạo đơn giao hàng cho ngày mai</p>
                    <form asp-action="GenerateDeliveryOrders" method="post" class="mt-2">
                        @Html.AntiForgeryToken()
                        <input type="date" name="targetDate" class="form-control mb-2" 
                               value="@DateOnly.FromDateTime(DateTime.Today.AddDays(1)).ToString("yyyy-MM-dd")">
                        <button type="submit" class="btn btn-warning">Generate</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Stats -->
    <div class="row g-4">
        <div class="col-md-3">
            <div class="card bg-primary text-white">
                <div class="card-body">
                    <h6>Today's Orders</h6>
                    <h3>-</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-success text-white">
                <div class="card-body">
                    <h6>Delivered</h6>
                    <h3>-</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-warning text-white">
                <div class="card-body">
                    <h6>In Progress</h6>
                    <h3>-</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-info text-white">
                <div class="card-body">
                    <h6>Planned</h6>
                    <h3>-</h3>
                </div>
            </div>
        </div>
    </div>
</div>
